import {
  to = aws_ecr_repository.repositories["datacenter-api"]
  id = "datacenter-api"
}

import {
  to = aws_ecr_repository.repositories["datacenter-backend"]
  id = "datacenter-backend"
}

import {
  to = aws_ecr_repository.repositories["executor"]
  id = "executor"
}

import {
  to = aws_ecr_repository.repositories["frontend"]
  id = "frontend"
}

import {
  to = aws_ecr_repository.repositories["market-data"]
  id = "market-data"
}

import {
  to = aws_ecr_repository.repositories["rest-api"]
  id = "rest-api"
}

import {
  to = aws_ecr_repository.repositories["trading"]
  id = "trading"
}

import {
  to = aws_ecr_repository.repositories["trading-api"]
  id = "trading-api"
}

import {
  to = aws_ecr_repository.repositories["websocket"]
  id = "websocket"
}

import {
  to = aws_ecr_repository.repositories["websocket"]
  id = "NATs"
}

import {
  to = aws_ecr_repository.repositories["websocket"]
  id = "notifications"
}

import {
  to = aws_ecr_repository.repositories["websocket"]
  id = "stats"
}
